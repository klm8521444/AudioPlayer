<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/sort"
        android:title="S"
        app:showAsAction="ifRoom">

        <item
            android:id="@+id/sortTitle"
            android:title="Сортировать по композиции"
            app:showAsAction="never"/>

        <item
            android:id="@+id/sortArtist"
            android:title="Сортировать по автору"
            app:showAsAction="never"/>

        <item
            android:id="@+id/sortAlbum"
            android:title="Сортировать альбомы"
            app:showAsAction="never"/>

        <item
            android:id="@+id/sortTime"
            android:title="Сортировать по времени"
            app:showAsAction="never"/>


    </group>

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_action_search"
        android:title="Search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />

    <item
        android:id="@+id/action_all"
        android:title="All"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_settings"
        android:title="Add playlist"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_playlist_add"/>

</menu>